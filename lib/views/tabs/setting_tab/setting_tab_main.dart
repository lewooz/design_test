import 'package:flutter/material.dart';

class SettingTabMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}
